apiVersion: v1
kind: Service
metadata:
  labels:
    app: rack-inventory-chat
  name: rack-inventory-chat
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: rack-inventory-chat
  sessionAffinity: None
  type: ClusterIP
